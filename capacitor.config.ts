import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rpjewellers.app',
  appName: 'Rp jewellers',
  webDir: 'dist/public'
};

export default config;
